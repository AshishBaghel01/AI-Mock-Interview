# AI Mock Interview Client

## Render and Firebase deployment

Google sign-in only works from domains allowed by Firebase. After deploying the client to Render, add the hostname of its public URL to Firebase:


## Environment variables

Configure these values in the Render client service before building ```

Configure this value in the Render API service when its frontend URL changes:


## Vite starter notes

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.




