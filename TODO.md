# TODO - Fix Render client build/deploy

- [ ] Fix Vite build error: `AuthModel.jsx` imports `../pages/auth` but page file is `Auth.jsx` (case-sensitive path).
- [ ] Update `client/src/components/AuthModel.jsx` import to `../pages/Auth`.
- [x] Re-run `npm run build` inside `client` to verify local build succeeds.
- [ ] (Optional) Add extra case-sensitive import checks if other similar issues exist.


